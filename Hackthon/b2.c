#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct Node
{
    int data;
    struct Node *next;
} Node;

int main()
{
    int choice;
    do
    {
        switch (choice)
        {
        case 1:
            /* code */
            break;

        default:
            break;
        }
    } while (choice);

    return 0;
}