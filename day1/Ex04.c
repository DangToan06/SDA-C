#include <stdio.h>

int main()
{

    // CÁCH 1
    // int sum = n * (n + 1) / 2;

    // ĐỘ PHỨC TẠP KHÔNG GIAN  LÀ O(1)

    // CÁCH 2
    // for (int i = 1; i <= n; i++)
    // {
    //     sum += i;
    // }

    // ĐỘ PHỨC TẠP KHÔNG GIAN LÀ O(n)

    return 0;
}